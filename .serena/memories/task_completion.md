When a task is completed, the following should be done:

1.  **Run linting and tests:**
    ```bash
    yarn lint
    yarn test
    yarn typecheck
    ```
2.  **Deploy the changes:**
    ```bash
    yarn deploy
    ```